# =============================================================================
# What's the 661 - Environment Variables
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Fill in the real values below
# 3. .env.local is gitignored and will NOT be committed
# =============================================================================

# Slack Webhook URL for receiving nominations
# Get this from: Slack App > Incoming Webhooks > Add New Webhook
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# OpenAI API Key for AI-powered showrunner insights
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Formspree Form ID for email capture
# 1. Go to https://formspree.io
# 2. Create a new form
# 3. Copy the Form ID from the URL or Integration tab (e.g. "mqkzvqrl")
VITE_FORMSPREE_ID=your_form_id_here

# =============================================================================
# CloudKit Web Services — iOS Prospects App Integration
# =============================================================================
# These enable the Vercel function to write nominations to CloudKit,
# where the iOS app reads them natively. All are optional — if not set,
# CloudKit is silently skipped and Slack still works.
#
# IMPORTANT: The iOS app reads Nominations from iCloud.com.vetraclients.app
# (not whatsthe661.prospects). Use that container.
#
# Setup:
# 1. Go to https://developer.apple.com/account → Certificates, Identifiers & Profiles
# 2. CloudKit container for Nominations: iCloud.com.vetraclients.app
# 3. In CloudKit Dashboard → your container → Server-to-Server Keys → Create key
# 4. Download the .pem file
# 5. For Vercel: use JSON format to avoid newline/trim issues:
#    CLOUDKIT_PRIVATE_KEY='{"privateKey":"-----BEGIN EC PRIVATE KEY-----\nYOUR_BASE64_LINES_HERE\n-----END EC PRIVATE KEY-----"}'
#    Replace actual newlines in the PEM with \n when pasting.
# 6. Or paste the raw PEM (with real newlines) — our parser handles both.

# CloudKit container (Nominations live in vetraclients.app)
CLOUDKIT_CONTAINER_ID=iCloud.com.vetraclients.app

# Key ID from CloudKit Dashboard → Server-to-Server Keys
CLOUDKIT_KEY_ID=your-cloudkit-key-id-here

# PEM private key — JSON format recommended for Vercel (avoids trailing newline issues):
# CLOUDKIT_PRIVATE_KEY='{"privateKey":"-----BEGIN EC PRIVATE KEY-----\n...\n-----END EC PRIVATE KEY-----"}'
CLOUDKIT_PRIVATE_KEY=

# "development" or "production"
CLOUDKIT_ENVIRONMENT=development
